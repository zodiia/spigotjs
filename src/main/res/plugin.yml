name: ${project.name}
version: ${project.version}
main: ${plugin.main}
author: Zodiak
website: ${project.url}
api-version: ${plugin.api-version}
commands:
  sjs:
    aliases: [spigotjs, spigotjavascript, js, javascript]
    description: Main command for SpigotJS management
  jseval:
    description: "Shortcut command for /js eval"
  jsget:
    description: "Shortcut command for /js get"
